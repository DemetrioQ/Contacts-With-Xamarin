<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ContactsWithXamarin.Views"
             x:Class="ContactsWithXamarin.Views.EditContactPage"
             >
    <ContentPage.Content>
        <StackLayout>
            <Image Source="{Binding After.Image}" HorizontalOptions="Center"></Image>
            <Entry Text="{Binding After.FirstName}" Placeholder="First Name"></Entry>
            <Entry Text="{Binding After.LastName}" Placeholder="Last Name"></Entry>
            <Entry Text="{Binding After.Phone}" Placeholder="Phone Number" Keyboard="Telephone"></Entry>
            <Button Text="Done" VerticalOptions="EndAndExpand" Command="{Binding DoneCommand}"></Button>
        </StackLayout>  
    </ContentPage.Content>
</ContentPage>