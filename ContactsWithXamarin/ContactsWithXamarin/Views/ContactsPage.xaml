<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ContactsWithXamarin.Views.ContactsPage"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core"
             ios:Page.UseSafeArea="true">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add"  Command="{Binding AddCommand}"/>
    </ContentPage.ToolbarItems>
    <ListView HasUnevenRows="True" Header="" ItemsSource="{Binding Contacts}">
        <ListView.HeaderTemplate>
            <DataTemplate>
                <Label Text="Contacts" FontSize="25" FontAttributes="Bold" HorizontalOptions="Center" TextColor="Black"></Label>
            </DataTemplate>
        </ListView.HeaderTemplate>
        <ListView.ItemTemplate>
            
        </ListView.ItemTemplate>
    </ListView>
    

</ContentPage>
